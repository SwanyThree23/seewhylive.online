{
  "type": "object",
  "name": "User",
  "title": "User",
  "properties": {
    "role": {
      "type": "string",
      "description": "The role of the user in the app",
      "enum": [
        "admin",
        "user"
      ]
    },
    "email": {
      "type": "string",
      "description": "The email of the user"
    },
    "full_name": {
      "type": "string",
      "description": "The full name of the user"
    },
    "avatar_url": {
      "type": "string",
      "description": "User profile picture URL"
    },
    "bio": {
      "type": "string",
      "description": "User biography"
    },
    "points": {
      "type": "number",
      "default": 0,
      "description": "Reward points earned"
    },
    "badges": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Achievement badges"
    },
    "referral_code": {
      "type": "string",
      "description": "Personal referral code"
    },
    "referred_by": {
      "type": "string",
      "description": "User who referred them"
    }
  },
  "required": [
    "referred_by",
    "email",
    "avatar_url",
    "full_name",
    "referral_code",
    "badges",
    "bio"
  ],
  "rls": {
    "create": {},
    "read": {},
    "update": {
      "$or": [
        {
          "id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}